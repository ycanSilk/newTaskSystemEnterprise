### 执行计划：实现完整闭环的图片上传功能

**目标**：按照项目架构实现从组件到API的完整图片上传功能，确保各层正确调用和集成。

#### 1. 移除useEphemeralImageManager.ts中的token处理
- **文件**：`src/hooks/useEphemeralImageManager.ts`
- **操作**：
  - 移除`useUserStore`的导入
  - 删除token获取逻辑
  - 简化API调用，移除X-Token头
  - 确保组件仅负责上传，不处理认证

#### 2. 标准化imagesUploadHandler.ts实现
- **文件**：`src/api/handlers/imagesUpload/imagesUploadHandler.ts`
- **操作**：
  - 导入标准化axios实例 `apiClient`
  - 导入错误处理函数 `handleApiError` 和 `createErrorResponse`
  - 导入API端点 `UPLOAD_IMAGE_ENDPOINT`
  - 使用 `apiClient.post` 调用API
  - 使用 `UPLOAD_IMAGE_ENDPOINT` 作为请求路径
  - 实现正确的错误处理

#### 3. 确保API路由正确集成
- **文件**：`src/app/api/imagesUpload/route.ts`
- **操作**：
  - 确保正确调用 `imagesUploadHandler`
  - 标记为动态路由

#### 4. 验证组件集成
- **文件**：`src/components/imagesUpload/ImageUpload.tsx`
- **操作**：
  - 确保组件正确调用 `/api/imagesUpload` 路由
  - 确保 `onImagesChange` 回调包含上传后的URL

**预期结果**：
- 组件仅负责上传操作，不处理认证
- API处理器使用标准化axios调用远程API
- 正确引用API端点定义
- 实现完整的上传闭环：组件 → Next.js路由 → API处理器 → 远程API → 返回结果

**项目架构和技术栈**：
- 分层架构：React组件 → Next.js API路由 → API处理器 → 标准化axios客户端 → 远程API
- 技术栈：React, Next.js, TypeScript, axios, Tailwind CSS
- 状态管理：zustand
- API设计：RESTful API，使用X-Token认证
- 文件结构：清晰的模块化设计，按功能划分目录